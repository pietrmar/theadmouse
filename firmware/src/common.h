#pragma once

// A generic enum for an AXIS that can be used in any context where it makes sense
enum axis {
	AXIS_X = 0,
	AXIS_Y = 1,
};

enum dir {
	DIR_UP		= 0,
	DIR_DOWN	= 1,
	DIR_LEFT	= 2,
	DIR_RIGHT	= 3,
};
